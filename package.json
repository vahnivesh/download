{
  "name": "clipcut-server",
  "version": "1.0.0",
  "description": "ClipCut yt-dlp download server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && pip install yt-dlp --break-system-packages || pip3 install yt-dlp"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
